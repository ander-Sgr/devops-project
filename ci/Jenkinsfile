pipeline {
    agent any

    environment {
        REGISTRY = 'andersgr' // Tu usuario en Docker Hub
        IMAGE_NAME = 'myproject' // Nombre de la imagen
        DOCKER_CREDENTIALS_ID = 'docker-hub-credentials' // ID único de credenciales
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'develop', url: 'https://github.com/user/repo.git'
            }
        }
        stage('Lint') {
            agent {
                docker { image 'envdev:latest' }
            }
            steps {
                sh 'flake8 app/ tests/'
            }
        }
        stage('Test') {
            agent {
                docker { image 'envdev:latest' }
            }
            steps {
                sh 'pytest --cov=app tests/'
            }
        }
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t ${REGISTRY}/${IMAGE_NAME}:${env.BUILD_NUMBER} .'
            }
        }
        stage('Push Docker Image') {
            when {
                branch develop
            }
            steps {
                withCredentials([usernamePassword(credentialsId: "${DOCKER_CREDENTIALS_ID}", usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASSWORD')]) {
                    sh '''
                        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
                        docker push ${REGISTRY}/${IMAGE_NAME}:${env.BUILD_NUMBER}
                    '''
                }
            }
        }
    }
}
